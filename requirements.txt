# torch + numpy ship in the rocm/pytorch image; these are the extras.
# Installed into ./.pydeps by `./run.sh setup` so they persist across container restarts.
numpy>=1.24
pyyaml>=6.0
tqdm>=4.65
scikit-learn>=1.3
lightgbm>=4.1
xgboost>=2.0
tensorboard>=2.14
# optional:
# catboost>=1.2
