name: cnn_bigru_coarse
model: cnn_bigru
label_space: coarse11
data: {max_per_class: 15000, val_max_per_class: 4000}
train: {epochs: 20, batch_size: 256, lr: 1.0e-3, weight_decay: 0.05, augment: true, gbflip_sigma: 0.01, num_workers: 8}
model_args: {channels: 96, gru_hidden: 128, gru_layers: 2}
notes: "Reference baseline (the 87.8%-wall architecture) on CORRECT labels."
